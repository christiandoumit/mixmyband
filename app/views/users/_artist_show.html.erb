<div class="container" style="text-align: center;">
  <h1><%= @user.artist_name %></h1>
  <h2><%= @user.artist_type %></h2>

  <div>
    <p>Available for <%= @user.event_type.capitalize %></p>
  </div>

  <p>$<%= @user.price %>/hour</p>
  <div>
    <button class="w3-button w3-round-large">Book me</button>
    <button class="w3-button w3-round-large">Send a message</button>
  </div>

  <div class="avatar row mt-3">
    <img src="<%= @user.avatar%>" alt="avatar" style="border-radius: 50%" height="100">
  </div>


  <h1 class="title">Audio</h1>
    <% if @user.audio_urls.count > 0 %>
      <div class="audio">
        <div id="carousel-audio" class="carousel slide" data-ride="carousel" data-interval="false">
          <div class="carousel-inner">
            <div class="carousel-item active">
              <iframe height="300" scrolling="no" frameborder="no" allow="autoplay" src="<%= @user.audio_urls.first.url %>"></iframe>
              <!-- <audio src="<%# @user.audio_urls.first.url %>" height="250" controls></audio> -->
            </div>
            <% @user.audio_urls[1..-1].each do |audio_url| %>
              <div class="carousel-item">
                <iframe height="300" scrolling="no" frameborder="no" allow="autoplay" src="<%= audio_url.url %>"></iframe>
                <!-- <audio src="<%# @user.audio_urls.first.url %>" height="250" controls></audio> -->
              </div>
            <% end %>
          </div>
          <a class="carousel-control-prev" href="#carousel-audio" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
          </a>
          <a class="carousel-control-next" href="#carousel-audio" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
          </a>
        </div>
      </div>
    <% end %>

  <h1 class="title">Videos</h1>
  <% if @user.youtube_urls.count > 0 %>
    <div class="video">
      <div id="carousel-video" class="carousel slide" data-ride="carousel" data-interval="false">
        <div class="carousel-inner">
          <div class="carousel-item active">
            <iframe height="250" src="<%= @user.youtube_urls.first.url %>" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
          </div>

          <% @user.youtube_urls[1..-1].each do |youtube_url| %>
            <div class="carousel-item">
              <iframe height="250" src="<%= youtube_url.url %>" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            </div>
          <% end %>
        </div>
        <a class="carousel-control-prev" href="#carousel-video" role="button" data-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carousel-video" role="button" data-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Next</span>
        </a>
      </div>
    </div>
  <% end %>

  <h1 class="title">Photos</h1>
  <% if @user.images.count > 0 %>
    <div class="Images">
      <div id="carousel-photo" class="carousel slide" data-ride="carousel" data-interval="false">
        <div class="carousel-inner">
          <div class="carousel-item active">
            <img height="250" src="<%= @user.images.first.image_src %>"></img>
          </div>

          <% @user.images[1..-1].each do |image| %>
            <div class="carousel-item">
              <img height="250" src="<%= image.image_src %>"></img>
            </div>
          <% end %>
        </div>
        <a class="carousel-control-prev" href="#carousel-photo" role="button" data-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carousel-photo" role="button" data-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Next</span>
        </a>
      </div>
    </div>
  <% end %>
</div>
